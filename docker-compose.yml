version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      LISTEN_PORT: 8000
    env_file:
      - ./backend/py/.env
